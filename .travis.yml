language: node_js
node_js:
  - '14'
branches:
  only:
    - master
cache:
  directories:
    - node_modules
before_install:
  - npm update
install:
  - npm install
script:
  - npm test -- --coverage
  - npm run report-coverage
